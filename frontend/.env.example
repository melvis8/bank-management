# Base URL for API calls. Leave as /api in dev — the Vite proxy forwards it to the backend.
# Set to the full backend URL (e.g. https://api.example.com/api) for a separate-origin production deploy.
VITE_API_BASE_URL=/api
