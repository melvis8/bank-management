# Frontend environment — production (unified deploy)
# When the frontend is served by the same Express server as the backend,
# API calls use a relative /api path (same origin = no CORS).
# Leave VITE_API_BASE_URL empty or set to /api — the client.ts fallback handles it.
VITE_API_BASE_URL=/api
